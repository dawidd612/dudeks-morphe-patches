package app.morphe.extension.crimera.settings;

/** Compile-only Piko API. Never bundled into Instagram. */
public class BooleanSetting {
    public BooleanSetting(String key, Boolean defaultValue) { throw new UnsupportedOperationException(); }
}
